# BULK Exchange Python SDK

Minimal project scaffold for a future HTTP MVP of the BULK Exchange Python SDK.

## Structure

- `bulk_sdk/` - package source
- `tests/` - test package
- `examples/` - usage examples
